# Example env for 03-sentinel alerter
# Comma-separated allowed origins (include protocol + port)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://dashboard.example.com
# Optional override for server port
PORT=4000
